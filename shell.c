#include "shell.h"

int main(void)
{
	char command[126];

	while (true)
{
void display_prompt(void)
{
	stevefrancis_print("stevefrancis_shell$ ");
}
	/*	read_command(command, sizeof(command));*/
	/*	execute_command(command); */
	}

	return (0);

}
